meta {
  name: Get Orders Tampered Jwt
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/orders
  body: none
  auth: none
}

headers {
  content-type: application/json
  authorization: Bearer tampered.invalid.token
}

tests {
  test("Status code is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
