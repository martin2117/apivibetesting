meta {
  name: Delete Cart No Auth
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/cart/1
  body: none
  auth: none
}

headers {
  content-type: application/json
}

tests {
  test("Status code is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
