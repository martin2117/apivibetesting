meta {
  name: Put Cart Missing Quantity
  type: http
  seq: 19
}

put {
  url: {{baseUrl}}/cart/{{cartItemId}}
  body: json
  auth: none
}

headers {
  content-type: application/json
  authorization: Bearer {{authToken}}
}

body:json {
  {}
}

tests {
  test("Status code is 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
