meta {
  name: Get Cart No Auth
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/cart
  body: none
  auth: none
}

headers {
  content-type: application/json
}

tests {
  test("Status code is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
