meta {
  name: Get Products Invalid Category
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/products?category=invalid-category
  body: none
  auth: none
}

headers {
  content-type: application/json
}

tests {
  test("Status code is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
